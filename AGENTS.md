# AGENTS.md

This file provides guidance to agents when working with E2E tests in this repository.

## アプリケーション概要

このアプリは **E2Eリグレッションテスト用のダミーWebアプリケーション**（カード・利用明細確認アプリ）です。

- **対象プラットフォーム**: Webブラウザ（`http://localhost:3000`）
- **技術スタック**: Next.js (App Router) + React + TypeScript
- **主な機能**: ログイン、カード一覧・フィルタリング、利用明細（ソート・月切替）、ポイント交換、プロフィール変更
- **テスト用アカウント**: `test@example.com` / `password123`
- **Seedデータ**: カード3枚、利用明細18件（3月12件・2月6件）、ポイント3,200、プロフィール（愛美衛門）

アプリはローカルで動作し、JSONファイルによる固定Seedデータを使用するため、毎回同じ状態でテストできます。

---

## E2Eテスト（Playwright MCP — Snapshot モード）ガイド — 必読

このアプリのE2Eテストは **Playwright MCP（Snapshot モード）** を使って実施します。
Snapshot モードでは、スクリーンショットや座標ではなく、**アクセシビリティツリー（`browser_snapshot`）** から取得した **`ref` 値** を使って要素を特定・操作します。

### 1. テスト開始前の確認事項

テスト実施前に、**必ず以下を確認**すること：

- Playwright MCP サーバーが起動しているか
- `http://localhost:3000` でアプリが起動しているか
- `browser_navigate` で `http://localhost:3000` を開き、ログイン画面が表示されるか

**確認せずにテストを開始してはいけません。**

### 2. Snapshot モードの基本原則（厳守）

#### 2.1 操作の流れ

すべてのブラウザ操作は以下のサイクルで行うこと：

1. **`browser_snapshot`** でアクセシビリティツリーを取得する
2. ツリーから対象要素の **`ref` 値**（例: `s1e35`）を特定する
3. `ref` 値を使って **`browser_click`** / **`browser_type`** 等で操作する
4. 操作後に再度 **`browser_snapshot`** を取得し、期待通りの状態になったことを確認する

**このサイクルを省略してはいけません。**

#### 2.2 `ref` 値のキャッシュ禁止

- `ref` 値は **取得したスナップショットに対してのみ有効**
- ページ遷移・クリック・入力など、DOMが変化する操作の後は `ref` が無効になる
- **操作のたびに `browser_snapshot` を取り直し、最新の `ref` を使うこと**
- 以前のスナップショットの `ref` を使い回してはいけない

#### 2.3 スクリーンショットの位置づけ

- **`browser_take_screenshot`** は視覚的な証跡・確認用途のみに使用する
- スクリーンショットから座標を読み取って操作してはいけない
- 要素の特定と操作には必ず `browser_snapshot` + `ref` を使うこと

### 3. 主要ツールと使い分け

#### 状態確認（操作前後に必ず実行）

| ツール                    | 用途                                                                            |
| ------------------------- | ------------------------------------------------------------------------------- |
| `browser_snapshot`        | アクセシビリティツリーを取得し、要素の `ref` を特定する。**すべての操作の起点** |
| `browser_take_screenshot` | 視覚的な証跡を記録する（操作には使用しない）                                    |

#### 要素操作（`ref` を指定して実行）

| ツール                  | 用途                     | 主要パラメータ                                              |
| ----------------------- | ------------------------ | ----------------------------------------------------------- |
| `browser_click`         | 要素をクリック           | `ref`, `element`（説明用）                                  |
| `browser_type`          | テキスト入力             | `ref`, `text`, `submit`（Enter送信）, `slowly`（1文字ずつ） |
| `browser_select_option` | ドロップダウン選択       | `ref`, `values`                                             |
| `browser_hover`         | ホバー                   | `ref`                                                       |
| `browser_fill_form`     | 複数フィールドを一括入力 | `fields`                                                    |

#### ナビゲーション

| ツール                  | 用途                                   |
| ----------------------- | -------------------------------------- |
| `browser_navigate`      | URLを指定して遷移                      |
| `browser_navigate_back` | ブラウザの戻るボタン                   |
| `browser_press_key`     | キーボード操作（`Enter`, `Escape` 等） |

#### 待機・ダイアログ

| ツール                  | 用途                                          |
| ----------------------- | --------------------------------------------- |
| `browser_wait_for`      | テキストの出現/消失を待つ、または指定秒数待機 |
| `browser_handle_dialog` | alert/confirm/prompt ダイアログの操作         |

### 4. 操作のベストプラクティス

- **ナビゲーション後は `browser_snapshot`**: 画面遷移後は必ずスナップショットを取り、遷移先が正しいことを確認する
- **入力後は `browser_snapshot`**: テキスト入力後にスナップショットを取り、値が反映されたことを確認する
- **`browser_wait_for` で非同期処理を待つ**: ローディングやアニメーション完了を待ってからスナップショットを取る
- **`browser_type` の `submit: true`**: 入力後にEnterを押す場合は `submit: true` を使う（`browser_press_key` を別途呼ぶ必要なし）
- **ログイン後の画面遷移**: ログイン後はナビゲーションバーのリンクを `browser_click` で操作する。`browser_navigate` でURLを直接指定すると localStorage がリセットされる場合がある
- **複数フィールドの入力**: `browser_fill_form` を使うと効率的

### 5. エラー発生時の対応（必須）

**エラーが発生したら、以下を必ず実施する：**

1. **`browser_snapshot`** を実行して現在のアクセシビリティツリーを確認する
2. **`browser_take_screenshot`** で視覚的な状態を記録する
3. エラーの原因を特定する（要素が見つからない / `ref` が無効 / 画面遷移の失敗 / タイミングの問題）
4. 必要に応じて `browser_wait_for` で待機してからスナップショットを取り直し、最新の `ref` で操作をリトライする

**古い `ref` でリトライしてはいけない。必ずスナップショットを取り直すこと。**

### 6. テストシナリオ実行時の注意

- 各テストシナリオは**独立して**実行できるようにする
- テスト開始時は `browser_navigate` で `http://localhost:3000` を開き、**ログイン画面から**スタートする
- テスト結果は「合格/不合格」を明確にし、不合格の場合はスクリーンショットとともに具体的な差分を報告する
- **出力言語**: テスト実行中の途中経過・状況説明・判定結果はすべて **日本語** で出力すること

---

## Seedデータ

### カード (`src/data/cards.json`)

| ID       | カード名           | ブランド   | 末尾4桁 | 利用限度額  | 利用額      |
| -------- | ------------------ | ---------- | ------- | ----------- | ----------- |
| card-001 | スタンダードカード | VISA       | 1234    | 300,000 JPY | 45,800 JPY  |
| card-002 | ゴールドカード     | Mastercard | 5678    | 500,000 JPY | 123,400 JPY |
| card-003 | プレミアムカード   | JCB        | 9012    | 200,000 JPY | 12,000 JPY  |

### 利用明細 (`src/data/transactions.json`)

- 2026年3月：**12件**（金額範囲: 320円〜32,000円）
- 2026年2月：**6件**
- 合計：18件

### ユーザー (`src/data/user.json`)

- お名前: `愛美衛門`
- メールアドレス: `test@example.com`
- パスワード: `password123`
- ポイント残高: `3,200`
- 電話番号: `090-1234-5678`

## ナビゲーション

ナビゲーションバーに以下のリンクが常時表示される：

- `カード一覧` → `/cards`
- `利用明細` → `/transactions`
- `ポイント交換` → `/points`
- `プロフィール` → `/profile`
